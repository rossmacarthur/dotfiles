# sheldon configuration file
#
# See https://github.com/rossmacarthur/sheldon

[templates]
alias = { value = '{{ name }}() { source {{ file }} }', each = true }
function = { value = 'ln -sf "{{ file }}" "{{ root }}/functions/{{ name }}"', each = true }
prompt_setup = { value = 'ln -sf "{{ file }}" "{{ root }}/functions/prompt_{{ name }}_setup"', each = true }
prompt_load = "promptinit\nprompt {{ name }}"

[plugins.async]
github = "mafredri/zsh-async"
apply = ["function"]
use = ["{{ name }}.zsh"]

[plugins.pure]
github = "rossmacarthur/pure"
apply = ["prompt_setup", "prompt_load"]

[plugins.autoenv]
github = "zpm-zsh/autoenv"
branch = "master"

[plugins.copybuffer]
github = "robbyrussell/oh-my-zsh"
dir = "plugins/{{ name }}"

[plugins.docker]
remote = "https://raw.githubusercontent.com/docker/cli/master/contrib/completion/zsh/_docker"
apply = ["fpath"]

[plugins.docker-compose]
remote = "https://raw.githubusercontent.com/docker/compose/master/contrib/completion/zsh/_docker-compose"
apply = ["fpath"]

[plugins.dotfiles]
local = "~/.zsh/plugins"

# The `git` plugin requires `compinit` to have been called before being sourced.
[plugins.compinit]
inline = "compinit"

[plugins.git]
github = "robbyrussell/oh-my-zsh"
dir = "plugins/{{ name }}"

[plugins.oh-my-zsh]
github = "robbyrussell/oh-my-zsh"
dir = "lib"
use = ["completion.zsh", "key-bindings.zsh", "git.zsh", "history.zsh"]

[plugins.pip]
github = "robbyrussell/oh-my-zsh"
dir = "plugins/{{ name }}"
apply = ["fpath"]

[plugins.wd]
github = "mfaerevaag/wd"
tag = "v0.4.6"
apply = ["alias", "fpath"]

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"
tag = "v0.5.2"
use = ["{{ name }}.zsh"]
